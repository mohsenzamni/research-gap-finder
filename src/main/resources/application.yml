spring:
  datasource:
    url: jdbc:h2:mem:researchgap;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate.ddl-auto: update
    open-in-view: false
  servlet.multipart:
    max-file-size: 20MB
    max-request-size: 20MB
springdoc.swagger-ui.path: /swagger-ui.html
